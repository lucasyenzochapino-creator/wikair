rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /aircraft/{aircraftId} {
      allow read: if true;
      // Para producción, escribe solo desde una herramienta admin o usuario autenticado con rol.
      allow write: if false;
    }
  }
}
